FROM k8s-elixir-1.6:latest

WORKDIR /app
COPY . .

ARG DB_DATEBASE
ENV DB_DATEBASE ${DB_DATEBASE}

ARG DB_USER
ENV DB_USER ${DB_USER}

ARG DB_PASSWORD
ENV DB_PASSWORD ${DB_PASSWORD}

ARG DB_HOST
ENV DB_HOST ${DB_HOST}

ARG DB_PORT
ENV DB_PORT ${DB_PORT}

ARG PORT
ENV PORT ${PORT}
